# docs — Feature Specs

TDD spec documents. Each describes one deliverable unit of work, used as input to `./tdd.sh red docs/<spec>.md`.

These are historical records of what was built. Read `LAST_TOUCH.md` for current state instead.

## Housekeeping

To reduce root-level context pollution for Claude hooks, legacy planning docs live under `docs/archive/` rather than repository root.

