#include "replay/buffers/InstrumentBuffer.hpp"

// This file intentionally left almost empty: all implementation
// is in the header for simplicity (inline methods).
// CMake needs a .cpp, so we provide an empty translation unit.
